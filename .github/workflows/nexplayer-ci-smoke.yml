name: NexPlayer CI Smoke

on:
  push:
    branches: [nexplayer-ci-v04]
  workflow_dispatch:

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo runner-ok
